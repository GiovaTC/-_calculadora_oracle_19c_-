CREATE TABLE OPERACIONES_OP
(
    ID NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NUMERO1 NUMBER(18,2),
    NUMERO2 NUMBER(18,2),
    OPERACION VARCHAR2(30),
    RESULTADO NUMBER(18,2),
    FECHA_REGISTRO DATE DEFAULT SYSDATE,

    CONSTRAINT PK_OPERACIONES_OP
        PRIMARY KEY(ID)
);

COMMIT;